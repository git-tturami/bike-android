package com.gitturami.bike.view.main.state

enum class State {
    PREPARE,
    SET_START,
    SET_FINISH,
    SELECT_CATEGORY,
    SELECT_WAYPOINT,
    HALF_WAYPOINT_SHEET,
    FULL_WAYPOINT_SHEET,
    POST_SELECT
}