package com.gitturami.bike.view.main.state

enum class State {
    PREPARE,
    SHOW_START,
    SET_START,
    SHOW_FINISH,
    SET_FINISH,
    SELECT_CATEGORY,
    SELECT_WAYPOINT,
    POST_SELECT_WAYPOINT,
    SELECTED_WAYPOINT
}