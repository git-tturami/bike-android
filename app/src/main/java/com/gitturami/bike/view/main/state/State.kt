package com.gitturami.bike.view.main.state

enum class State {
    PREPARE,
    SET_START,
    SET_FINISH,
    SELECT_CATEGORY,
    MIN_WAYPOINT_SHEET,
    HALF_WAYPOINT_SHEET,
    FULL_WAYPOINT_SHEET
}